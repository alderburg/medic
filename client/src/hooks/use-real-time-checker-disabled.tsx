// SISTEMA ANTIGO DESATIVADO - SUBSTITUÍDO PELO ENTERPRISE
// Este hook foi substituído pelo sistema enterprise de notificações
// Mantido apenas para referência

export function useRealTimeChecker() {
  // Sistema desativado - Enterprise system is handling notifications
  console.log('⚠️ Sistema antigo desativado - usando enterprise notifications');
  return;
}