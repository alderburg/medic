# Database Configuration
# PostgreSQL connection string for Locaweb external database
DATABASE_URL=postgresql://agendamedic:Dr19122010@@agendamedic.postgresql.dbaas.com.br:5432/agendamedic

# Database connection details (if needed separately)
PGHOST=agendamedic.postgresql.dbaas.com.br
PGPORT=5432
PGUSER=agendamedic
PGPASSWORD=Dr19122010@@
PGDATABASE=agendamedic

# Application Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration (for authentication)
JWT_SECRET=your_jwt_secret_key

# Session Configuration
SESSION_SECRET=your_session_secret_key